# Production configuration for AI Learning Bot
# Copy this file to .env.prod and fill in the values

# ===============================
# REQUIRED VARIABLES
# ===============================

# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=your_production_telegram_bot_token_here

# Grok API Key for content generation
GROK_API_KEY=your_grok_api_key_here

# ===============================
# DATABASE
# ===============================

# PostgreSQL settings
POSTGRES_DB=ai_learning_prod
POSTGRES_USER=ai_bot_prod
POSTGRES_PASSWORD=secure_database_password_change_me

# ===============================
# REDIS (CACHING)
# ===============================

# Redis password
REDIS_PASSWORD=secure_redis_password_change_me

# ===============================
# MONITORING AND LOGGING
# ===============================

# Grafana admin password
GRAFANA_PASSWORD=secure_grafana_password

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Timezone
TIMEZONE=Europe/Moscow

# ===============================
# ADDITIONAL SETTINGS
# ===============================

# Domain for webhook mode (if used)
WEBHOOK_DOMAIN=your-domain.com

# Ports for external access (if needed)
# HTTP_PORT=80
# HTTPS_PORT=443
# GRAFANA_PORT=3000
# PROMETHEUS_PORT=9090

# ===============================
# SECURITY
# ===============================

# Environment
ENVIRONMENT=production

# Database connection pool settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30

# ===============================
# DEPLOYMENT INSTRUCTIONS
# ===============================

# 1. Fill in all variables above
# 2. Run: docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
# 3. For monitoring: docker-compose -f docker-compose.prod.yml --env-file .env.prod --profile monitoring up -d
# 4. For webhook: docker-compose -f docker-compose.prod.yml --env-file .env.prod --profile webhook up -d

# Status check: docker-compose -f docker-compose.prod.yml ps
# Logs: docker-compose -f docker-compose.prod.yml logs -f bot
# Stop: docker-compose -f docker-compose.prod.yml down
